# 化学反应实时模拟接口文档

## 概述

本接口提供化学反应的实时模拟功能，通过 WebSocket 推送模拟过程中的状态变化，包括物质浓度、反应速率、方程式信息等。

**服务地址**: `ws://localhost:9209/chemistry/simulation/stream/{simulationId}`

**API 基础路径**: `/chemistry/simulation`

---

## 1. 启动实时模拟

### 接口信息

| 项目 | 说明 |
|------|------|
| 接口地址 | `/chemistry/simulation/start-realtime` |
| 请求方式 | `POST` |
| Content-Type | `application/json` |

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| substances | List | 是 | 反应物列表（1-4个） |
| substances[].id | Long | 是 | 物质ID |
| substances[].amount | BigDecimal | 是 | 输入值 |
| substances[].unit | String | 是 | 单位：g/mL/L/mol |
| substances[].concentration | String | 否 | 液体浓度（mol/L），仅液体需要 |
| catalysts | List | 否 | 催化剂列表 |
| catalysts[].id | Long | 是 | 催化剂物质ID |
| catalysts[].amount | BigDecimal | 是 | 催化剂用量 |
| catalysts[].unit | String | 是 | 单位：g/mL |
| conditions | Object | 否 | 反应条件 |
| conditions.temperature | BigDecimal | 否 | 初始温度（K），默认298.15 |
| conditions.pressure | BigDecimal | 否 | 压力（Pa），默认101325 |
| conditions.ignited | Boolean | 否 | 是否点燃，默认false |
| conditions.ph | BigDecimal | 否 | pH值 |
| conditions.solvent | String | 否 | 溶剂 |
| totalVolume | BigDecimal | 是 | 反应体系总体积（L） |
| timeStep | BigDecimal | 否 | 模拟时间步长（秒），默认0.1 |
| maxIterations | Integer | 否 | 最大迭代次数，默认1000 |
| rateThreshold | BigDecimal | 否 | 反应速率阈值，默认0.000001 |

### 请求示例

```json
{
  "substances": [
    {
      "id": 29,
      "amount": 1,
      "unit": "g"
    },
    {
      "id": 34,
      "amount": 10,
      "unit": "ml",
      "concentration": "55.5"
    }
  ],
  "totalVolume": 1.0,
  "conditions": {
    "temperature": 298.15,
    "pressure": 101325
  }
}
```

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
| simulationId | String | 模拟ID（UUID） |
| status | String | 模拟状态：RUNNING/COMPLETED/FAILED/CANCELLED |
| estimatedDuration | Double | 预计持续时间（秒） |
| estimatedEndTime | LocalDateTime | 预计结束时间 |
| wsUrl | String | WebSocket 连接地址 |

### 响应示例

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "simulationId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "status": "RUNNING",
    "estimatedDuration": 10.0,
    "estimatedEndTime": "2025-02-05T14:30:45",
    "wsUrl": "ws://localhost:9209/chemistry/simulation/stream/a1b2c3d4-e5f6-7890-abcd-ef1234567890"
  }
}
```

---

## 2. WebSocket 推送数据

### 连接地址

```
ws://localhost:9209/chemistry/simulation/stream/{simulationId}
```

**注意**: WebSocket 直接连接到 glxt 模块端口 9209，不经过网关。

### 连接时机

1. 先调用 `/start-realtime` 接口获取 `simulationId`
2. 立即建立 WebSocket 连接（服务端会等待 500ms 便于连接建立）
3. 接收实时推送的模拟状态

### 消息类型

#### 2.1 STATE_UPDATE - 状态更新

模拟过程中定期推送当前状态。

**消息结构**:

```json
{
  "type": "STATE_UPDATE",
  "timestamp": 1738762845000,
  "data": {
    "simulationId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "currentTime": 1.5,
    "temperature": 298.15,
    "pressure": 101325,
    "volume": 1.0,
    "substances": [...],
    "activeReactions": [...],
    "phenomena": {...}
  }
}
```

#### 2.2 SIMULATION_COMPLETED - 模拟完成

所有反应完成时推送。

```json
{
  "type": "SIMULATION_COMPLETED",
  "timestamp": 1738762850000,
  "data": {
    "simulationId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "totalTime": 10.0,
    "finalState": {...}
  }
}
```

#### 2.3 SIMULATION_FAILED - 模拟失败

模拟出错时推送。

```json
{
  "type": "SIMULATION_FAILED",
  "timestamp": 1738762830000,
  "data": {
    "simulationId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "error": "错误信息描述"
  }
}
```

---

## 3. 状态数据详情

### 3.1 物质状态 (substances)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long | 物质ID |
| name | String | 物质名称 |
| formula | String | 化学式 |
| amount | BigDecimal | 当前物质的量（mol） |
| concentration | BigDecimal | 当前浓度（mol/L） |
| state | String | 物质状态：solid/liquid/gas/aqueous |
| color | String | 颜色 |
| molarMass | BigDecimal | 摩尔质量（g/mol） |

**示例**:

```json
{
  "substances": [
    {
      "id": 29,
      "name": "钠",
      "formula": "Na",
      "amount": 0.0435,
      "concentration": 0.0435,
      "state": "solid",
      "color": "银白色",
      "molarMass": 22.99
    },
    {
      "id": 34,
      "name": "水",
      "formula": "H₂O",
      "amount": 0.555,
      "concentration": 0.555,
      "state": "liquid",
      "color": "无色",
      "molarMass": 18.02
    }
  ]
}
```

### 3.2 活跃反应 (activeReactions)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| equationId | Long | 方程式ID |
| equationText | String | 方程式文本 |
| equationHtml | String | 方程式HTML（带下标格式） |
| reactionRate | String | 反应速率描述：instant/very_fast/fast/moderate/slow/very_slow |
| currentRate | BigDecimal | 当前反应速率（mol/s） |
| progress | BigDecimal | 反应进度（0-1） |
| status | String | 反应状态：EXECUTING=正在执行，MATCHED_ONLY=仅匹配未执行 |
| enthalpyChange | BigDecimal | 焓变（kJ/mol），负值表示放热 |
| rateConstantK | BigDecimal | 速率常数 k |
| reactionOrder | String | 反应级数 |
| temperatureDependent | String | 温度依赖：NONE/LOW/MEDIUM/HIGH |
| activationEnergy | BigDecimal | 活化能（kJ/mol） |
| reactants | Array | 反应物消耗信息 |
| products | Array | 产物生成信息 |

**反应物消耗 (reactants)**:

| 字段名 | 类型 | 说明 |
|--------|------|------|
| substanceId | Long | 物质ID |
| name | String | 物质名称 |
| consumptionRate | BigDecimal | 消耗速率（mol/s） |

**产物生成 (products)**:

| 字段名 | 类型 | 说明 |
|--------|------|------|
| substanceId | Long | 物质ID |
| name | String | 物质名称 |
| productionRate | BigDecimal | 生成速率（mol/s） |

**示例**:

```json
{
  "activeReactions": [
    {
      "equationId": 6,
      "equationText": "2Na + 2H₂O → 2NaOH + H₂↑",
      "equationHtml": "2Na + 2H<sub>2</sub>O → 2NaOH + H<sub>2</sub>↑",
      "reactionRate": "instant",
      "currentRate": 0.000044,
      "progress": 0.5,
      "status": "EXECUTING",
      "enthalpyChange": -368.0,
      "rateConstantK": 1000.0,
      "reactionOrder": "first",
      "temperatureDependent": "MEDIUM",
      "reactants": [
        {
          "substanceId": 29,
          "name": "钠",
          "consumptionRate": 0.000088
        },
        {
          "substanceId": 34,
          "name": "水",
          "consumptionRate": 0.000088
        }
      ],
      "products": [
        {
          "substanceId": 16,
          "name": "氢氧化钠",
          "productionRate": 0.000088
        },
        {
          "substanceId": 35,
          "name": "氢气",
          "productionRate": 0.000044
        }
      ]
    }
  ]
}
```

### 3.3 反应现象 (phenomena)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| colorChange | Object | 颜色变化 |
| gasEvolution | Array | 气体产生 |
| precipitation | Array | 沉淀 |
| temperatureChange | BigDecimal | 温度变化（累计） |
| description | String | 现象描述 |

**示例**:

```json
{
  "phenomena": {
    "colorChange": {
      "before": "银白色",
      "current": "无色透明",
      "description": "钠从银白色逐渐溶解，溶液变为无色"
    },
    "gasEvolution": [
      {
        "formula": "H₂",
        "name": "氢气",
        "rate": 0.000044
      }
    ],
    "temperatureChange": 5.2,
    "description": "钠与水剧烈反应，产生气泡，放出热量"
  }
}
```

---

## 4. 完整流程示例

### 4.1 步骤流程

```
1. POST /chemistry/simulation/start-realtime
   ↓
2. 获取 simulationId 和 wsUrl
   ↓
3. 建立 WebSocket 连接
   ↓
4. 接收 STATE_UPDATE 消息（多次）
   ↓
5. 接收 SIMULATION_COMPLETED 消息
   ↓
6. 关闭 WebSocket 连接
```

### 4.2 代码示例（JavaScript）

```javascript
// 1. 启动模拟
const startResponse = await fetch('http://localhost:8080/chemistry/simulation/start-realtime', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    substances: [
      { id: 29, amount: 1, unit: 'g' },
      { id: 34, amount: 10, unit: 'ml', concentration: '55.5' }
    ],
    totalVolume: 1.0
  })
});

const startData = await startResponse.json();
const { simulationId, wsUrl } = startData.data;

// 2. 建立 WebSocket 连接
const ws = new WebSocket(wsUrl);

ws.onopen = () => {
  console.log('WebSocket 已连接');
};

ws.onmessage = (event) => {
  const message = JSON.parse(event.data);

  switch (message.type) {
    case 'STATE_UPDATE':
      console.log('状态更新:', message.data);
      // 处理物质状态、活跃反应等
      break;

    case 'SIMULATION_COMPLETED':
      console.log('模拟完成:', message.data);
      ws.close();
      break;

    case 'SIMULATION_FAILED':
      console.error('模拟失败:', message.data.error);
      ws.close();
      break;
  }
};

ws.onerror = (error) => {
  console.error('WebSocket 错误:', error);
};

ws.onclose = () => {
  console.log('WebSocket 已关闭');
};
```

---

## 5. 错误码说明

| 错误码 | 说明 |
|--------|------|
| 200 | 请求成功 |
| 400 | 请求参数错误 |
| 500 | 服务器内部错误 |

---

## 6. 注意事项

1. **WebSocket 连接时机**: 调用 `/start-realtime` 后应立即建立 WebSocket 连接，服务端会等待 500ms 便于连接建立。

2. **端口说明**: WebSocket 直接连接到 glxt 模块端口 9209，不经过网关 8080。

3. **物质单位**: 支持的输入单位包括：
   - `g` - 克（固体）
   - `mol` - 摩尔
   - `L` - 升（液体）
   - `mL` - 毫升（液体）

   液体单位需要提供 `concentration` 参数（浓度，单位 mol/L）。

4. **反应状态说明**:
   - `EXECUTING`: 反应正在执行（速率高于阈值）
   - `MATCHED_ONLY`: 匹配到方程式但未执行（速率低于阈值）

5. **链式反应**: 模拟器支持链式反应，产物会自动加入物质池参与后续反应。

---

## 7. 附录：常用物质ID参考

| ID | 名称 | 化学式 | 状态 |
|----|------|--------|------|
| 4 | 铁 | Fe | solid |
| 13 | 硫酸 | H₂SO₄ | liquid |
| 16 | 氢氧化钠 | NaOH | solid |
| 29 | 钠 | Na | solid |
| 34 | 水 | H₂O | liquid |
| 35 | 氢气 | H₂ | gas |
